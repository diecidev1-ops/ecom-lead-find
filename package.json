{
  "name": "ecom-lead-find",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Estrae lead da profili di riferimento Instagram/TikTok tramite Apify",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.22.0"
  }
}
